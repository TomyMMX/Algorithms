using System;

namespace Algorithms.Trees
{
		public class AATree<TKey, TValue> where TKey : IComparable<TKey>
        {
       /* Node<TKey, TValue> root;
        Node<TKey, TValue> sentinel;
        Node<TKey, TValue> deleted;

        public AATree()
        {
                root = sentinel = new Node<TKey, TValue>();
                deleted = null;
        }*/
	}
}

