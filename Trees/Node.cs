using System;

namespace Algorithms.Trees
{
		/*public class Node<TKey, TValue>
        {
                // node internal data
                public int level;
                public Node<TKey, TValue>[] children= new Node<TKey, TValue>[2];
                
                // user data
                public TKey key;
                public TValue value;

                // constuctor for the sentinel node
                public Node()
                {
                        this.level = 0;
                        this.children[0] = this;
                        this.children[1] = this;
                }

                // constuctor for regular nodes (that all start life as leaf nodes)
                public Node(TKey key, TValue value, Node<TKey, TValue> air)
                {
                        this.level = 1;
						this.children[0] = air;
						this.children[1] = air;
                        this.key = key;
                        this.value = value;
                }
        }*/

}

